{
  "name":"context",
  "version":"0.0.0",
  "scripts": {
    "test": "ccargs -p test/test.json -t -c -v",
    "format": "clang-format -i --style=Webkit src/*.h test/src/*.cpp"
  },
  "devDependencies": {
    "traverse": "git+ssh://git-codecommit.us-west-1.amazonaws.com/v1/repos/traverse",
    "drop": "git+ssh://git-codecommit.us-west-1.amazonaws.com/v1/repos/drop"
  }
}
